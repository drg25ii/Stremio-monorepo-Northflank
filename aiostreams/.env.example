# URL-ul public Northflank (HTTPS), fără :port
BASE_URL=https://REPLACE_WITH_NORTHFLANK_URL

# OBLIGATORIU: 64 hex (generează cu: openssl rand -hex 32)
SECRET_KEY=REPLACE_WITH_64_HEX

# Optional
LOG_LEVEL=info

# Optional: protejează instalarea/folosirea addon-ului
# ADDON_PASSWORD=change_me

# Optional: DB (dacă vrei să schimbi default)
# DATABASE_URI=sqlite://./data/db.sqlite
