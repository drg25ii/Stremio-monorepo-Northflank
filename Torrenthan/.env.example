# Copiază în .env și modifică după nevoie:
#   cp .env.example .env

# Portul pe care rulează Uvicorn în container (și pe care îl expui din container)
PORT=7002

# (Opțional) Portul pe host, dacă vrei să îl faci configurabil în compose (ex: 7002/17002)
HOST_PORT=7002

# (Opțional) Tag pentru imagine (util dacă vrei versionare)
IMAGE_TAG=latest
